# 重大工程叠加分析系统 - 快速启动指南

## 一键启动（推荐）

### Windows用户

#### 1. 启动后端服务
打开命令提示符（CMD），执行：
```cmd
cd D:\tianshui_project\backend
python manage.py runserver
```

保持此窗口打开，后端服务运行在 http://localhost:8000

#### 2. 启动GeoServer
- 双击GeoServer安装目录的启动脚本
- 或者启动GeoServer Windows服务
- 访问 http://localhost:8080/geoserver/web 验证

#### 3. 启动前端服务
打开新的命令提示符窗口，执行：
```cmd
cd D:\tianshui_project\frontend
npm run dev
```

前端服务启动后，浏览器会自动打开或显示访问地址（通常是 http://localhost:5173）

#### 4. 访问叠加分析页面
在浏览器中访问：http://localhost:5173/overlay-analysis

## 快速测试流程

### Step 1: 打开页面
访问 http://localhost:5173/overlay-analysis

✅ **预期效果**：
- 页面显示"重大工程叠加分析与监控"标题
- 地图自动加载，中心位于天水市
- 左上角显示图层控制面板
- 右上角显示缩放按钮
- 右下角显示坐标

### Step 2: 查看图层
地图自动加载三个图层：
- 🌿 生态指数栅格（彩色显示生态状况）
- 💰 经济数据矢量（显示各区域GDP）
- 🏗️ 工程项目矢量（显示生态修复工程）

✅ **测试图层控制**：
1. 点击左上角图层控制面板
2. 取消勾选"生态指数栅格" → 图层消失
3. 重新勾选 → 图层显示

### Step 3: 点击地图获取信息
随意点击地图上的任意位置

✅ **预期效果**：
弹窗显示该位置的：
- 📍 经纬度坐标
- 🌿 生态指数（如果有）
- 💰 经济数据（如果有）
- 🏗️ 工程项目（如果有）
- ⚠️ 风险分析
- 💡 决策建议

### Step 4: 测试风险识别

**测试场景1：极高风险区域**
点击满足以下条件的区域：
- 生态指数显示为"较差"（红色）
- GDP显示为高值（红色）
- 无工程项目

✅ **预期结果**：
- 风险分析显示：🔴 **极高风险**
- 决策建议："建议尽快部署生态修复工程，优先考虑生态敏感区域保护"

**测试场景2：低风险区域**
点击已有工程项目的区域，或生态指数良好的区域

✅ **预期结果**：
- 风险分析显示：🟢 **已有治理工程** 或 **生态状况良好**
- 决策建议："建议持续监控工程效果"或"建议继续保持良好的生态环境"

## 浏览器控制台检查

按 F12 打开开发者工具，切换到 Console 标签

### 地图初始化日志
应该看到：
```
✅ OverlayAnalysis 页面已加载
🗺️ 初始化重大工程叠加分析地图...
✅ 地图初始化完成
📍 中心点: [105.7, 34.6]
📦 已加载图层数: 5
```

### 点击地图后的日志
应该看到：
```
🖱️ 地图点击事件触发
📍 点击坐标 (经纬度): [105.xxxx, 34.xxxx]
🔍 开始获取图层信息...
📊 GetFeatureInfo结果: {ecology: {...}, economy: {...}, engineering: {...}}
🌿 生态数据: {value: xxx, level: "..."}
💰 经济数据: {admin_name: "...", GDP: xxx}
🏗️ 工程数据: [...]
✅ 弹窗已打开
```

## 常见问题快速解决

### ❌ 问题：图层不显示

**解决方案**：
1. 检查GeoServer是否运行：访问 http://localhost:8080/geoserver
2. 检查控制台是否有红色错误信息
3. 刷新页面（Ctrl+F5）

### ❌ 问题：点击地图无反应

**解决方案**：
1. 检查浏览器控制台是否有错误
2. 检查GeoServer是否正常运行
3. 确认图层已发布到GeoServer

### ❌ 问题：弹窗显示"无数据"

**可能原因**：
1. 点击的位置确实没有数据（正常情况）
2. GeoServer图层数据未正确发布
3. GetFeatureInfo请求失败

**验证方法**：
- 在浏览器开发者工具的Network标签中查看GetFeatureInfo请求
- 查看请求的响应内容

## 演示数据说明

如果没有真实数据，系统会显示：
- ❌ "该位置无生态指数数据"
- ❌ "该位置无经济数据"
- ❌ "该位置无工程项目"

这是正常的，表示GetFeatureInfo请求成功但该位置没有数据。

要获得完整的演示效果，需要：
1. 确保GeoServer中已发布三个图层
2. 图层包含有效的数据
3. 点击有数据覆盖的区域

## 核心业务流程总结

```
用户打开页面
    ↓
地图自动加载三个WMS图层
    ↓
用户点击地图任意位置
    ↓
系统发送3个GetFeatureInfo请求
    ↓
解析返回的JSON数据
    ↓
计算风险等级（基于生态指数、GDP、工程项目）
    ↓
生成决策建议
    ↓
弹窗显示完整的叠加分析结果
```

## 下一步

1. ✅ 验证所有图层正常显示
2. ✅ 测试点击功能正常
3. ✅ 验证风险分析逻辑正确
4. 📝 根据实际数据调整风险阈值
5. 📝 添加更多决策建议场景

---

**提示**：第一次启动可能需要等待GeoServer完全启动（约1-2分钟）



