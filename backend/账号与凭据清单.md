# 账号与凭据交接清单

本文档用于交付现场登记账号、密码、服务地址与重置状态。

注意事项：

- 不要把真实密码提交到 Git 仓库。
- 本文件建议仅在交付现场填写，并在交付后转移到受控位置保存。
- 所有默认密码、示例密码、弱口令都应在交付前重置。

## 一、Django 系统账号

当前可确认的本地账号：

| 账号类型 | 用户名 | 邮箱 | 角色 | 状态 | 当前密码是否应保留在仓库 |
|---|---|---|---|---|---|
| 管理员 | `admin` | `admin@example.com` | `admin` | 启用 | 否 |
| 普通用户 | `user` | `user@example.com` | `user` | 启用 | 否 |

交付填写区：

| 用户名 | 交付对象/使用人 | 现场重置后密码 | 是否已首次登录后修改 | 备注 |
|---|---|---|---|---|
| `admin` |  |  |  |  |
| `user` |  |  |  |  |

建议：

- 至少保留一个管理员账号。
- 交付前统一重置所有本地测试账号密码。
- 如不需要多个管理员，建议删除多余管理员账号。

## 二、数据库连接信息

当前项目存在数据库连接配置，请现场确认并重置：

| 项目 | 当前值 | 交付现场填写 |
|---|---|---|
| 数据库类型 | `PostgreSQL` |  |
| 数据库名 | `tianshui` |  |
| 数据库用户 | `postgres` |  |
| 数据库密码 | 请勿写入仓库 |  |
| 主机 | `localhost` |  |
| 端口 | `5433` |  |

说明：

- 若正式环境改用其他库名、用户或端口，请同步更新 `backend/.env`。
- 数据库密码建议由甲方现场重新设置。

## 三、GeoServer 凭据

当前代码存在 GeoServer 配置入口，请现场确认：

| 项目 | 当前值 | 交付现场填写 |
|---|---|---|
| GeoServer URL | `http://localhost:8080/geoserver` |  |
| 用户名 | `admin` |  |
| 密码 | 请勿写入仓库 |  |
| 工作区 | `tianshuipy` |  |

建议：

- 交付前修改 GeoServer 管理员密码。
- 修改后同步更新 Django 环境配置。

## 四、Django 运行密钥

| 项目 | 说明 | 交付现场填写 |
|---|---|---|
| `SECRET_KEY` | Django 生产环境必须使用独立随机密钥 |  |

建议：

- 生产环境不要继续使用开发默认密钥。
- 使用随机生成的高强度字符串。

## 五、交付确认

| 检查项 | 是否完成 | 备注 |
|---|---|---|
| 已重置全部测试账号密码 |  |  |
| 已重置数据库密码 |  |  |
| 已重置 GeoServer 密码 |  |  |
| 已设置新的 `SECRET_KEY` |  |  |
| 已更新 `backend/.env` |  |  |
| 已验证管理员可登录 |  |  |
| 已验证普通用户可登录 |  |  |

## 六、推荐操作命令

创建新管理员：

```powershell
python manage.py create_superuser --username admin --email admin@example.com
```

如果需要在现场重置用户密码，可进入 Django shell 处理：

```powershell
python manage.py shell
```

然后执行：

```python
from users.models import User
u = User.objects.get(username='admin')
u.set_password('现场新密码')
u.save()
```
