# 快速命令参考

## 一、当前启动方式

### 1. 一键启动前后端

在项目根目录执行：

```powershell
.\start_dev.ps1
```

说明：

- 前端固定地址：`http://localhost:3000`
- 后端固定地址：`http://127.0.0.1:8000`
- 若端口已被占用，脚本会直接提示，不会自动漂移到其他端口

---

### 2. 单独启动后端

在项目根目录执行：

```powershell
cd backend
.\start_dev.ps1
```

当前后端启动脚本优先使用：

```text
C:\Users\当前用户名\miniforge3\envs\tianshui-gis\python.exe
```

如果实际解释器不在这个位置，可设置环境变量：

```powershell
$env:TIANSHUI_PYTHON="你的 python.exe 完整路径"
```

后端实际运行地址：

```text
http://127.0.0.1:8000
```

---

### 3. 单独启动前端

在项目根目录执行：

```powershell
cd frontend
npm run dev -- --host 127.0.0.1 --port 3000
```

前端实际运行地址：

```text
http://localhost:3000
```

---

## 二、常用 Django 命令

以下命令建议在 `backend` 目录下执行。

### 1. 启动开发服务

```powershell
& 'C:\Users\当前用户名\miniforge3\envs\tianshui-gis\python.exe' manage.py runserver 127.0.0.1:8000
```

### 2. 数据库迁移

```powershell
& 'C:\Users\当前用户名\miniforge3\envs\tianshui-gis\python.exe' manage.py makemigrations
& 'C:\Users\当前用户名\miniforge3\envs\tianshui-gis\python.exe' manage.py migrate
```

### 3. 检查配置

```powershell
& 'C:\Users\当前用户名\miniforge3\envs\tianshui-gis\python.exe' manage.py check
```

### 4. 打开 Django shell

```powershell
& 'C:\Users\当前用户名\miniforge3\envs\tianshui-gis\python.exe' manage.py shell
```

### 5. 创建或重置管理员

```powershell
& 'C:\Users\当前用户名\miniforge3\envs\tianshui-gis\python.exe' manage.py create_superuser
```

---

## 三、管理员创建建议

交付前不要保留固定测试口令。

推荐命令：

```powershell
python manage.py create_superuser --username admin --email admin@example.com
```

说明：

- 如果不传 `--password`，命令会自动生成安全随机密码
- 建议首次登录后立即修改

---

## 四、安全开关

生产/交付环境推荐在 `backend/.env` 中确认以下配置：

```text
ALLOW_PUBLIC_USER_REGISTRATION=False
ALLOW_ANONYMOUS_ANALYSIS_UPLOADS=False
ALLOW_ANONYMOUS_BUSINESS_LAYER_ADMIN=False
ALLOW_ANONYMOUS_OVERLAY_ADMIN=False
ALLOW_PUBLIC_FEEDBACK_MANAGEMENT=False
```

如果只是现场演示，确实需要匿名操作，可以临时改为 `True`。

---

## 五、前端常用命令

以下命令建议在 `frontend` 目录下执行。

### 1. 安装依赖

```powershell
npm install
```

### 2. 启动开发环境

```powershell
npm run dev -- --host 127.0.0.1 --port 3000
```

### 3. 构建生产包

```powershell
npm run build
```

---

## 六、GeoServer 相关

### 1. 默认配置

```text
GeoServer URL: http://localhost:8080/geoserver
用户名：admin
密码：请以现场实际配置为准
```

### 2. 查看 GeoServer 状态

```text
http://127.0.0.1:8000/api/v1/environment/spatial/geoserver/status/
```

### 3. 查看系统健康状态

```text
http://127.0.0.1:8000/api/v1/environment/system/health/
```

---

## 七、用户管理与权限

当前系统已支持管理员与普通用户基础管理。

管理员登录后可在前端主页使用“用户管理”功能进行：

- 新增用户
- 修改用户信息
- 修改角色
- 启用/禁用用户
- 删除用户
- 分配模块权限

当前已预留的权限模块包括：

- `remote_sensing`
- `ecological_index`
- `overlay_analysis`
- `climate_monitoring`
- `feedback`
- `business_layers`
- `user_management`

---

## 八、常见问题

### 1. 前端打不开

先确认：

- `frontend/node_modules` 是否存在
- `3000` 端口是否被其他程序占用

### 2. 后端打不开

先确认：

- GIS 环境是否存在：`C:\Users\当前用户名\miniforge3\envs\tianshui-gis\python.exe`
- `8000` 端口是否被占用

### 3. 上传文件后结果不显示

建议依次检查：

- 当前文件是否适用于对应分析模块
- GeoServer 是否正常运行
- 后端日志是否有报错
- 当前任务是否已完成

### 4. 气候栅格无法自动识别变量

若文件名只有年份，如 `2023年.tif`，系统可能无法自动判断是温度、降水、湿度还是风速。

这时需要在前端手动选择变量后再开始分析。

---

## 九、建议使用顺序

推荐平时这样启动和使用：

1. 在项目根目录执行 `.\start_dev.ps1`
2. 打开前端：`http://localhost:3000`
3. 管理员先登录
4. 如需管理用户，先进入“用户管理”
5. 再按模块进入遥感、生态指数、气候监测或叠加分析页面
