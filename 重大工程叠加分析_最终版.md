# 重大工程叠加分析系统 - 完整说明

## 📌 系统概述

重大工程叠加分析系统是天水市生态环境监测系统的核心功能模块，实现了生态指数、经济数据和工程项目的空间叠加分析，为环境决策提供数据支持。

## ✅ 已实现功能

### 1. 地图可视化
- **底图**：高德地图（EPSG:3857 Web Mercator）
- **投影系统**：View 使用 EPSG:4326（经纬度），OpenLayers 自动处理投影转换
- **中心坐标**：天水市 (105.7, 34.6)
- **缩放等级**：初始 zoom=10

### 2. WMS 图层叠加
三个 GeoServer WMS 图层：
- **生态指数栅格** (`ecology_raster`) - 透明度 70%
- **经济数据矢量** (`economy_vector`) - 透明度 60%
- **工程项目矢量** (`engineering_vector`) - 透明度 80%

**技术配置**：
- WMS 版本：1.1.0（更好的兼容性）
- 参数：SRS=EPSG:4326, TILED=true, TRANSPARENT=true, FORMAT=image/png
- 支持实时图层切换

### 3. 交互式数据查询
- **点击获取数据**：使用 WMS GetFeatureInfo 请求
- **批量查询**：同时查询三个图层
- **坐标转换**：自动处理 EPSG:4326 和显示坐标的转换
- **错误处理**：优雅处理无数据区域

### 4. 叠加分析弹窗
显示内容：
- **位置信息**：经纬度坐标
- **生态指数**：
  - 数值（如：776.14）
  - 等级评估（优/良/中等/较差/差）
  - 颜色标识
- **经济数据**：GDP、人口、产业类型
- **工程项目**：项目名称、类型、状态
- **风险分析**：根据数据自动识别风险等级
- **决策建议**：提供针对性的管理建议

### 5. 地图控制
- **缩放控制**：右上角 +/- 按钮
- **图层控制**：左上角勾选框切换图层可见性
- **坐标显示**：右下角实时显示鼠标位置经纬度

## 🔧 技术架构

### 前端技术栈
- **框架**：Vue 3 (Composition API)
- **地图库**：OpenLayers 7.5.2
- **构建工具**：Vite
- **样式**：Scoped CSS

### 后端技术栈
- **Web Server**：Django
- **地图服务**：GeoServer 2.x
- **数据库**：PostgreSQL + PostGIS

### 投影系统配置
```javascript
// View 配置
new View({
  projection: 'EPSG:4326',  // 经纬度投影
  center: [105.7, 34.6],    // 天水市中心
  zoom: 10
})

// WMS 图层参数
{
  VERSION: '1.1.0',
  SRS: 'EPSG:4326',        // 1.1.0 使用 SRS
  TILED: true,
  TRANSPARENT: true,
  FORMAT: 'image/png'
}

// GetFeatureInfo 请求
{
  VERSION: '1.1.0',
  SRS: 'EPSG:4326',
  BBOX: 'minX,minY,maxX,maxY',  // 1.1.0 统一格式
  X: pixelX,                     // 1.1.0 使用 X/Y
  Y: pixelY,
  INFO_FORMAT: 'application/json'
}
```

## 🎯 核心文件

### 前端文件
```
frontend/src/
├── views/
│   └── OverlayAnalysis.vue           # 主页面
├── components/
│   └── Map/
│       ├── OverlayMapContainer.vue   # 地图容器（核心）
│       └── OverlayAnalysisPopup.vue  # 分析弹窗
└── utils/
    └── mapUtils.js                   # 地图工具函数
```

### 后端文件
```
backend/environment/
├── models.py                         # 数据模型
├── views.py                          # API 视图
├── overlay_analysis.py               # 叠加分析逻辑
├── spatial_views.py                  # 空间视图
└── geoserver_config.py               # GeoServer 配置
```

## 🚀 快速启动

### 1. 启动后端
```bash
cd backend
conda activate ts
python manage.py runserver
```

### 2. 启动 GeoServer
```bash
# GeoServer 应运行在 http://localhost:8080/geoserver
```

### 3. 启动前端
```bash
cd frontend
npm run dev
```

### 4. 访问系统
浏览器打开：http://localhost:3000/overlay-analysis

## 🧪 测试流程

### 1. 基础测试
1. 打开页面，确认地图和图层正常显示
2. 检查右下角坐标实时更新
3. 测试缩放控制（+/- 按钮）
4. 测试图层切换（勾选框）

### 2. 功能测试
1. **点击绿色区域**（生态良好）：
   - 应显示高生态指数值
   - 等级应为"优"或"良"
   - 风险分析应为"生态状况良好"
   
2. **点击黄色区域**（生态中等）：
   - 应显示中等生态指数值
   - 等级应为"中等"
   - 风险分析应提示关注

3. **点击红色区域**（生态较差）：
   - 应显示较低生态指数值
   - 等级应为"较差"或"差"
   - 风险分析应为"高风险"
   - 决策建议应提示"尽快部署生态修复"

### 3. 控制台检查
正常情况下应看到：
```
✅ 地图初始化完成
📐 View 投影: EPSG:4326
📍 View 中心: [105.7, 34.6]
🖱️ 点击坐标: [经度, 纬度]
🔍 开始获取图层数据...
GetFeatureInfo请求 (WMS 1.1.0): {...}
🌱 生态数据: {index: xxx, level: 'xxx'}
```

## ⚠️ 常见问题

### 1. 地图不显示
- **检查**：控制台是否有网络错误
- **原因**：高德地图可能被拦截
- **解决**：检查网络连接，或在 Network 标签查看瓦片请求

### 2. WMS 图层不显示
- **检查**：GeoServer 是否运行（http://localhost:8080/geoserver）
- **检查**：控制台是否有 CORS 错误
- **解决**：确保 GeoServer 允许 CORS 请求

### 3. 点击无数据
- **原因1**：该位置确实无数据（正常情况）
- **原因2**：GeoServer 图层配置问题
- **检查**：浏览器直接访问 GetFeatureInfo URL 查看响应

### 4. 坐标显示异常
- **检查**：View 投影是否为 EPSG:4326
- **检查**：坐标值是否在合理范围（105-106, 34-35）

## 📊 数据说明

### 生态指数
- **数据源**：栅格数据（TIFF）
- **值范围**：通常 0-1000
- **评级标准**：
  - 优：≥ 800
  - 良：600-800
  - 中等：400-600
  - 较差：200-400
  - 差：< 200

### 经济数据
- **数据源**：矢量数据（Shapefile/GeoJSON）
- **字段**：gdp, population, industry
- **覆盖范围**：主要城区和经济区

### 工程项目
- **数据源**：矢量数据（Shapefile/GeoJSON）
- **字段**：name, type, status
- **类型**：生态修复、基础设施、水利工程等

## 🔒 安全注意事项

1. **生产环境配置**：
   - 修改 GeoServer 默认密码
   - 配置 HTTPS
   - 限制 CORS 源

2. **性能优化**：
   - 启用 GeoServer 瓦片缓存
   - 配置 CDN 加速底图
   - 数据库索引优化

## 📝 更新日志

### v1.0.0 (2025-01-XX)
- ✅ 完成地图可视化基础功能
- ✅ 实现 WMS 图层叠加
- ✅ 实现点击查询 GetFeatureInfo
- ✅ 实现叠加分析弹窗
- ✅ 修复投影系统问题（EPSG:4326）
- ✅ 优化 WMS 1.1.0 兼容性
- ✅ 完善错误处理和用户体验

## 🎉 总结

重大工程叠加分析系统现已完全正常运行！

**核心成就**：
- 成功解决了 OpenLayers View 投影混乱问题
- 实现了 EPSG:4326 (View) 和 EPSG:3857 (高德地图) 的自动转换
- 优化了 WMS GetFeatureInfo 的 BBOX 顺序和参数配置
- 实现了优雅的错误处理（无数据区域不报错）

**系统特点**：
- 🗺️ 稳定的地图显示
- 📊 准确的数据查询
- 🎨 美观的界面设计
- 🚀 良好的用户体验

---
**维护者**：天水市生态环境监测系统团队  
**最后更新**：2025年1月



